services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_USER: adplatform
      POSTGRES_PASSWORD: adplatform
      POSTGRES_DB: adplatform
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data

  backend:
    build: ./backend
    env_file: ./backend/.env
    ports:
      - "8000:8000"
    depends_on:
      - postgres

volumes:
  pg_data: